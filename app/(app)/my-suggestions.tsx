import React from "react";
import MySuggestions from "../../src/screens/MySuggestions";

/**
 * My Suggestions Route
 *
 * Part of Permission System v4.2 - allows users to view and track
 * their submitted edit suggestions across all statuses (pending, approved, rejected).
 */
export default function MySuggestionsScreen() {
  return <MySuggestions />;
}
