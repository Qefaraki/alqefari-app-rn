/**
 * Broadcast Notification Route
 *
 * Expo Router screen for sending broadcast notifications
 * Accessible only to super admins
 */

import React from 'react';
import BroadcastNotificationScreen from '../../../src/screens/admin/BroadcastNotificationScreen';

export default function BroadcastNotificationRoute() {
  return <BroadcastNotificationScreen />;
}
